---
alwaysApply: true
---

# API Endpoints Reference (HMS)

## Overview
This document defines the complete `/api/v1` surface for the HMS backend.
All module endpoints **must** use the `/api/v1/` base path prefix as required by `api.mdc` and `api-versioning.mdc`.

## Conventions
- CRUD:
  - `GET /<resource>`: list (paginated)
  - `GET /<resource>/{id}`: get one
  - `POST /<resource>`: create
  - `PUT /<resource>/{id}`: update
  - `DELETE /<resource>/{id}`: soft delete
- Nested: `/<resource>/{id}/<subresource>`
- Action endpoints allowed: `POST /<resource>/{id}/<action>` (must be validated and authorized)
- Query params use `snake_case` and support `page`, `limit`, `sort_by`, `order`

## 0. System Health Endpoints (Unversioned)
These endpoints are **system endpoints** and are not mounted under `/api/v1` (see `health-checks.mdc`):
```javascript
GET /health
GET /ready
GET /live
```

## 1. Authentication & Sessions
```javascript
/api/v1/auth/
├── POST /login                     // Authenticate user and return JWT
├── POST /logout                    // Logout and invalidate session
├── POST /refresh                   // Refresh JWT token
├── POST /register                  // Register new user (if enabled)
├── POST /verify-email              // Verify email with token/code
├── POST /verify-phone              // Verify phone with token/code
├── POST /resend-verification       // Resend verification email/SMS
├── POST /forgot-password           // Send password reset email/SMS
├── POST /reset-password            // Reset password with token
├── POST /change-password           // Change password (authenticated)
└── GET /me                         // Get current user info

/api/v1/user-sessions/
├── GET /                           // List user sessions with pagination
├── GET /{id}                       // Get user session by ID
└── DELETE /{id}                    // Revoke session
```

## 2. Tenancy, Facilities & Organization
```javascript
/api/v1/tenants/
├── GET /                           // List tenants with pagination
├── POST /                          // Create tenant
├── GET /{id}                       // Get tenant by ID
├── PUT /{id}                       // Update tenant
└── DELETE /{id}                    // Soft delete tenant

/api/v1/facilities/
├── GET /                           // List facilities with pagination
├── POST /                          // Create facility
├── GET /{id}                       // Get facility by ID
├── PUT /{id}                       // Update facility
├── DELETE /{id}                    // Soft delete facility
└── GET /{id}/branches              // Get facility branches

/api/v1/branches/
├── GET /                           // List branches with pagination
├── POST /                          // Create branch
├── GET /{id}                       // Get branch by ID
├── PUT /{id}                       // Update branch
└── DELETE /{id}                    // Soft delete branch

/api/v1/departments/
├── GET /                           // List departments with pagination
├── POST /                          // Create department
├── GET /{id}                       // Get department by ID
├── PUT /{id}                       // Update department
├── DELETE /{id}                    // Soft delete department
└── GET /{id}/units                 // Get department units

/api/v1/units/
├── GET /                           // List units with pagination
├── POST /                          // Create unit
├── GET /{id}                       // Get unit by ID
├── PUT /{id}                       // Update unit
└── DELETE /{id}                    // Soft delete unit

/api/v1/rooms/
├── GET /                           // List rooms with pagination
├── POST /                          // Create room
├── GET /{id}                       // Get room by ID
├── PUT /{id}                       // Update room
└── DELETE /{id}                    // Soft delete room

/api/v1/wards/
├── GET /                           // List wards with pagination
├── POST /                          // Create ward
├── GET /{id}                       // Get ward by ID
├── PUT /{id}                       // Update ward
├── DELETE /{id}                    // Soft delete ward
└── GET /{id}/beds                  // Get ward beds

/api/v1/beds/
├── GET /                           // List beds with pagination
├── POST /                          // Create bed
├── GET /{id}                       // Get bed by ID
├── PUT /{id}                       // Update bed
└── DELETE /{id}                    // Soft delete bed

/api/v1/addresses/
├── GET /                           // List addresses with pagination
├── POST /                          // Create address
├── GET /{id}                       // Get address by ID
├── PUT /{id}                       // Update address
└── DELETE /{id}                    // Soft delete address

/api/v1/contacts/
├── GET /                           // List contacts with pagination
├── POST /                          // Create contact
├── GET /{id}                       // Get contact by ID
├── PUT /{id}                       // Update contact
└── DELETE /{id}                    // Soft delete contact
```

## 3. Users, Profiles & RBAC
```javascript
/api/v1/users/
├── GET /                           // List users with pagination
├── POST /                          // Create user
├── GET /{id}                       // Get user by ID
├── PUT /{id}                       // Update user
└── DELETE /{id}                    // Soft delete user

/api/v1/user-profiles/
├── GET /                           // List user profiles with pagination
├── POST /                          // Create user profile
├── GET /{id}                       // Get user profile by ID
├── PUT /{id}                       // Update user profile
└── DELETE /{id}                    // Soft delete user profile

/api/v1/roles/
├── GET /                           // List roles with pagination
├── POST /                          // Create role
├── GET /{id}                       // Get role by ID
├── PUT /{id}                       // Update role
└── DELETE /{id}                    // Soft delete role

/api/v1/permissions/
├── GET /                           // List permissions with pagination
├── POST /                          // Create permission
├── GET /{id}                       // Get permission by ID
├── PUT /{id}                       // Update permission
└── DELETE /{id}                    // Soft delete permission

/api/v1/role-permissions/
├── GET /                           // List role-permission mappings
├── POST /                          // Create role-permission mapping
├── GET /{id}                       // Get role-permission mapping
├── PUT /{id}                       // Update role-permission mapping
└── DELETE /{id}                    // Soft delete role-permission mapping

/api/v1/user-roles/
├── GET /                           // List user-role mappings
├── POST /                          // Create user-role mapping
├── GET /{id}                       // Get user-role mapping
├── PUT /{id}                       // Update user-role mapping
└── DELETE /{id}                    // Soft delete user-role mapping

/api/v1/api-keys/
├── GET /                           // List API keys with pagination
├── POST /                          // Create API key
├── GET /{id}                       // Get API key by ID
├── PUT /{id}                       // Update API key
└── DELETE /{id}                    // Soft delete API key

/api/v1/api-key-permissions/
├── GET /                           // List API key permissions
├── POST /                          // Create API key permission
├── GET /{id}                       // Get API key permission
├── PUT /{id}                       // Update API key permission
└── DELETE /{id}                    // Soft delete API key permission

/api/v1/user-mfas/
├── GET /                           // List user MFA configs
├── POST /                          // Create user MFA config
├── GET /{id}                       // Get user MFA by ID
├── PUT /{id}                       // Update user MFA
├── DELETE /{id}                    // Soft delete user MFA
├── POST /{id}/verify               // Verify MFA code
├── POST /{id}/enable               // Enable MFA
└── POST /{id}/disable              // Disable MFA

/api/v1/oauth-accounts/
├── GET /                           // List OAuth accounts
├── POST /                          // Link OAuth account
├── GET /{id}                       // Get OAuth account by ID
├── PUT /{id}                       // Update OAuth account
└── DELETE /{id}                    // Soft delete OAuth account
```

## 4. Patient Registry & Demographics
```javascript
/api/v1/patients/
├── GET /                           // List patients with pagination
├── POST /                          // Create patient
├── GET /{id}                       // Get patient by ID
├── PUT /{id}                       // Update patient
├── DELETE /{id}                    // Soft delete patient
├── GET /{id}/identifiers           // Get patient identifiers
├── GET /{id}/contacts              // Get patient contacts
├── GET /{id}/guardians             // Get patient guardians
├── GET /{id}/allergies             // Get patient allergies
├── GET /{id}/medical-histories     // Get patient medical history
└── GET /{id}/documents             // Get patient documents

/api/v1/patient-identifiers/
├── GET /                           // List patient identifiers
├── POST /                          // Create patient identifier
├── GET /{id}                       // Get patient identifier by ID
├── PUT /{id}                       // Update patient identifier
└── DELETE /{id}                    // Soft delete patient identifier

/api/v1/patient-contacts/
├── GET /                           // List patient contacts
├── POST /                          // Create patient contact
├── GET /{id}                       // Get patient contact by ID
├── PUT /{id}                       // Update patient contact
└── DELETE /{id}                    // Soft delete patient contact

/api/v1/patient-guardians/
├── GET /                           // List patient guardians
├── POST /                          // Create patient guardian
├── GET /{id}                       // Get patient guardian by ID
├── PUT /{id}                       // Update patient guardian
└── DELETE /{id}                    // Soft delete patient guardian

/api/v1/patient-allergies/
├── GET /                           // List patient allergies
├── POST /                          // Create patient allergy
├── GET /{id}                       // Get patient allergy by ID
├── PUT /{id}                       // Update patient allergy
└── DELETE /{id}                    // Soft delete patient allergy

/api/v1/patient-medical-histories/
├── GET /                           // List patient medical histories
├── POST /                          // Create patient medical history
├── GET /{id}                       // Get patient medical history by ID
├── PUT /{id}                       // Update patient medical history
└── DELETE /{id}                    // Soft delete patient medical history

/api/v1/patient-documents/
├── GET /                           // List patient documents
├── POST /                          // Upload patient document
├── GET /{id}                       // Get patient document by ID
├── PUT /{id}                       // Update patient document
└── DELETE /{id}                    // Soft delete patient document

/api/v1/consents/
├── GET /                           // List consents with pagination
├── POST /                          // Create consent
├── GET /{id}                       // Get consent by ID
├── PUT /{id}                       // Update consent
└── DELETE /{id}                    // Soft delete consent

/api/v1/terms-acceptances/
├── GET /                           // List terms acceptances with pagination
├── POST /                          // Create terms acceptance
├── GET /{id}                       // Get terms acceptance by ID
└── DELETE /{id}                    // Soft delete terms acceptance
```

## 5. Scheduling, Availability & Queues
```javascript
/api/v1/appointments/
├── GET /                           // List appointments with pagination
├── POST /                          // Create appointment
├── GET /{id}                       // Get appointment by ID
├── PUT /{id}                       // Update appointment
├── DELETE /{id}                    // Soft delete appointment
└── POST /{id}/cancel               // Cancel appointment

/api/v1/appointment-participants/
├── GET /                           // List appointment participants
├── POST /                          // Create appointment participant
├── GET /{id}                       // Get appointment participant by ID
├── PUT /{id}                       // Update appointment participant
└── DELETE /{id}                    // Soft delete appointment participant

/api/v1/appointment-reminders/
├── GET /                           // List appointment reminders
├── POST /                          // Create appointment reminder
├── GET /{id}                       // Get appointment reminder by ID
├── PUT /{id}                       // Update appointment reminder
└── DELETE /{id}                    // Soft delete appointment reminder

/api/v1/provider-schedules/
├── GET /                           // List provider schedules
├── POST /                          // Create provider schedule
├── GET /{id}                       // Get provider schedule by ID
├── PUT /{id}                       // Update provider schedule
└── DELETE /{id}                    // Soft delete provider schedule

/api/v1/availability-slots/
├── GET /                           // List availability slots
├── POST /                          // Create availability slot
├── GET /{id}                       // Get availability slot by ID
├── PUT /{id}                       // Update availability slot
└── DELETE /{id}                    // Soft delete availability slot

/api/v1/visit-queues/
├── GET /                           // List visit queues
├── POST /                          // Create visit queue entry
├── GET /{id}                       // Get visit queue entry by ID
├── PUT /{id}                       // Update visit queue entry
└── DELETE /{id}                    // Soft delete visit queue entry
```

## 6. Encounters & Clinical Documentation
```javascript
/api/v1/encounters/
├── GET /                           // List encounters with pagination
├── POST /                          // Create encounter
├── GET /{id}                       // Get encounter by ID
├── PUT /{id}                       // Update encounter
└── DELETE /{id}                    // Soft delete encounter

/api/v1/clinical-notes/
├── GET /                           // List clinical notes
├── POST /                          // Create clinical note
├── GET /{id}                       // Get clinical note by ID
├── PUT /{id}                       // Update clinical note
└── DELETE /{id}                    // Soft delete clinical note

/api/v1/diagnoses/
├── GET /                           // List diagnoses
├── POST /                          // Create diagnosis
├── GET /{id}                       // Get diagnosis by ID
├── PUT /{id}                       // Update diagnosis
└── DELETE /{id}                    // Soft delete diagnosis

/api/v1/procedures/
├── GET /                           // List procedures
├── POST /                          // Create procedure
├── GET /{id}                       // Get procedure by ID
├── PUT /{id}                       // Update procedure
└── DELETE /{id}                    // Soft delete procedure

/api/v1/vital-signs/
├── GET /                           // List vital signs
├── POST /                          // Create vital sign record
├── GET /{id}                       // Get vital sign by ID
├── PUT /{id}                       // Update vital sign
└── DELETE /{id}                    // Soft delete vital sign

/api/v1/care-plans/
├── GET /                           // List care plans
├── POST /                          // Create care plan
├── GET /{id}                       // Get care plan by ID
├── PUT /{id}                       // Update care plan
└── DELETE /{id}                    // Soft delete care plan

/api/v1/clinical-alerts/
├── GET /                           // List clinical alerts
├── POST /                          // Create clinical alert
├── GET /{id}                       // Get clinical alert by ID
├── PUT /{id}                       // Update clinical alert
└── DELETE /{id}                    // Soft delete clinical alert

/api/v1/referrals/
├── GET /                           // List referrals
├── POST /                          // Create referral
├── GET /{id}                       // Get referral by ID
├── PUT /{id}                       // Update referral
└── DELETE /{id}                    // Soft delete referral

/api/v1/follow-ups/
├── GET /                           // List follow-ups
├── POST /                          // Create follow-up
├── GET /{id}                       // Get follow-up by ID
├── PUT /{id}                       // Update follow-up
└── DELETE /{id}                    // Soft delete follow-up
```

## 7. Inpatient (IPD) & Bed Management
```javascript
/api/v1/admissions/
├── GET /                           // List admissions
├── POST /                          // Create admission
├── GET /{id}                       // Get admission by ID
├── PUT /{id}                       // Update admission
├── DELETE /{id}                    // Soft delete admission
└── POST /{id}/discharge            // Discharge patient

/api/v1/bed-assignments/
├── GET /                           // List bed assignments
├── POST /                          // Create bed assignment
├── GET /{id}                       // Get bed assignment by ID
├── PUT /{id}                       // Update bed assignment
└── DELETE /{id}                    // Soft delete bed assignment

/api/v1/ward-rounds/
├── GET /                           // List ward rounds
├── POST /                          // Create ward round
├── GET /{id}                       // Get ward round by ID
├── PUT /{id}                       // Update ward round
└── DELETE /{id}                    // Soft delete ward round

/api/v1/nursing-notes/
├── GET /                           // List nursing notes
├── POST /                          // Create nursing note
├── GET /{id}                       // Get nursing note by ID
├── PUT /{id}                       // Update nursing note
└── DELETE /{id}                    // Soft delete nursing note

/api/v1/medication-administrations/
├── GET /                           // List medication administrations
├── POST /                          // Create medication administration
├── GET /{id}                       // Get medication administration by ID
├── PUT /{id}                       // Update medication administration
└── DELETE /{id}                    // Soft delete medication administration

/api/v1/discharge-summaries/
├── GET /                           // List discharge summaries
├── POST /                          // Create discharge summary
├── GET /{id}                       // Get discharge summary by ID
├── PUT /{id}                       // Update discharge summary
└── DELETE /{id}                    // Soft delete discharge summary

/api/v1/transfer-requests/
├── GET /                           // List transfer requests
├── POST /                          // Create transfer request
├── GET /{id}                       // Get transfer request by ID
├── PUT /{id}                       // Update transfer request
└── DELETE /{id}                    // Soft delete transfer request
```

## 8. ICU & Critical Care
```javascript
/api/v1/icu-stays/
├── GET /                           // List ICU stays
├── POST /                          // Create ICU stay
├── GET /{id}                       // Get ICU stay by ID
├── PUT /{id}                       // Update ICU stay
└── DELETE /{id}                    // Soft delete ICU stay

/api/v1/icu-observations/
├── GET /                           // List ICU observations
├── POST /                          // Create ICU observation
├── GET /{id}                       // Get ICU observation by ID
├── PUT /{id}                       // Update ICU observation
└── DELETE /{id}                    // Soft delete ICU observation

/api/v1/critical-alerts/
├── GET /                           // List critical alerts
├── POST /                          // Create critical alert
├── GET /{id}                       // Get critical alert by ID
├── PUT /{id}                       // Update critical alert
└── DELETE /{id}                    // Soft delete critical alert
```

## 9. Theatre & Anesthesia
```javascript
/api/v1/theatre-cases/
├── GET /                           // List theatre cases
├── POST /                          // Create theatre case
├── GET /{id}                       // Get theatre case by ID
├── PUT /{id}                       // Update theatre case
└── DELETE /{id}                    // Soft delete theatre case

/api/v1/anesthesia-records/
├── GET /                           // List anesthesia records
├── POST /                          // Create anesthesia record
├── GET /{id}                       // Get anesthesia record by ID
├── PUT /{id}                       // Update anesthesia record
└── DELETE /{id}                    // Soft delete anesthesia record

/api/v1/post-op-notes/
├── GET /                           // List post-op notes
├── POST /                          // Create post-op note
├── GET /{id}                       // Get post-op note by ID
├── PUT /{id}                       // Update post-op note
└── DELETE /{id}                    // Soft delete post-op note
```

## 10. Laboratory (LIS)
```javascript
/api/v1/lab-tests/
├── GET /                           // List lab tests
├── POST /                          // Create lab test
├── GET /{id}                       // Get lab test by ID
├── PUT /{id}                       // Update lab test
└── DELETE /{id}                    // Soft delete lab test

/api/v1/lab-panels/
├── GET /                           // List lab panels
├── POST /                          // Create lab panel
├── GET /{id}                       // Get lab panel by ID
├── PUT /{id}                       // Update lab panel
└── DELETE /{id}                    // Soft delete lab panel

/api/v1/lab-orders/
├── GET /                           // List lab orders
├── POST /                          // Create lab order
├── GET /{id}                       // Get lab order by ID
├── PUT /{id}                       // Update lab order
└── DELETE /{id}                    // Soft delete lab order

/api/v1/lab-order-items/
├── GET /                           // List lab order items
├── POST /                          // Create lab order item
├── GET /{id}                       // Get lab order item by ID
├── PUT /{id}                       // Update lab order item
└── DELETE /{id}                    // Soft delete lab order item

/api/v1/lab-samples/
├── GET /                           // List lab samples
├── POST /                          // Create lab sample
├── GET /{id}                       // Get lab sample by ID
├── PUT /{id}                       // Update lab sample
└── DELETE /{id}                    // Soft delete lab sample

/api/v1/lab-results/
├── GET /                           // List lab results
├── POST /                          // Create lab result
├── GET /{id}                       // Get lab result by ID
├── PUT /{id}                       // Update lab result
└── DELETE /{id}                    // Soft delete lab result

/api/v1/lab-qc-logs/
├── GET /                           // List lab QC logs
├── POST /                          // Create lab QC log
├── GET /{id}                       // Get lab QC log by ID
├── PUT /{id}                       // Update lab QC log
└── DELETE /{id}                    // Soft delete lab QC log
```

## 11. Radiology (RIS / PACS)
```javascript
/api/v1/radiology-tests/
├── GET /                           // List radiology tests
├── POST /                          // Create radiology test
├── GET /{id}                       // Get radiology test by ID
├── PUT /{id}                       // Update radiology test
└── DELETE /{id}                    // Soft delete radiology test

/api/v1/radiology-orders/
├── GET /                           // List radiology orders
├── POST /                          // Create radiology order
├── GET /{id}                       // Get radiology order by ID
├── PUT /{id}                       // Update radiology order
└── DELETE /{id}                    // Soft delete radiology order

/api/v1/radiology-results/
├── GET /                           // List radiology results
├── POST /                          // Create radiology result
├── GET /{id}                       // Get radiology result by ID
├── PUT /{id}                       // Update radiology result
└── DELETE /{id}                    // Soft delete radiology result

/api/v1/imaging-studies/
├── GET /                           // List imaging studies
├── POST /                          // Create imaging study
├── GET /{id}                       // Get imaging study by ID
├── PUT /{id}                       // Update imaging study
└── DELETE /{id}                    // Soft delete imaging study

/api/v1/imaging-assets/
├── GET /                           // List imaging assets
├── POST /                          // Upload imaging asset
├── GET /{id}                       // Get imaging asset by ID
├── PUT /{id}                       // Update imaging asset
└── DELETE /{id}                    // Soft delete imaging asset

/api/v1/pacs-links/
├── GET /                           // List PACS links
├── POST /                          // Create PACS link
├── GET /{id}                       // Get PACS link by ID
├── PUT /{id}                       // Update PACS link
└── DELETE /{id}                    // Soft delete PACS link
```

## 12. Pharmacy
```javascript
/api/v1/drugs/
├── GET /                           // List drugs
├── POST /                          // Create drug
├── GET /{id}                       // Get drug by ID
├── PUT /{id}                       // Update drug
└── DELETE /{id}                    // Soft delete drug

/api/v1/drug-batches/
├── GET /                           // List drug batches
├── POST /                          // Create drug batch
├── GET /{id}                       // Get drug batch by ID
├── PUT /{id}                       // Update drug batch
└── DELETE /{id}                    // Soft delete drug batch

/api/v1/formulary-items/
├── GET /                           // List formulary items
├── POST /                          // Create formulary item
├── GET /{id}                       // Get formulary item by ID
├── PUT /{id}                       // Update formulary item
└── DELETE /{id}                    // Soft delete formulary item

/api/v1/pharmacy-orders/
├── GET /                           // List pharmacy orders
├── POST /                          // Create pharmacy order
├── GET /{id}                       // Get pharmacy order by ID
├── PUT /{id}                       // Update pharmacy order
└── DELETE /{id}                    // Soft delete pharmacy order

/api/v1/pharmacy-order-items/
├── GET /                           // List pharmacy order items
├── POST /                          // Create pharmacy order item
├── GET /{id}                       // Get pharmacy order item by ID
├── PUT /{id}                       // Update pharmacy order item
└── DELETE /{id}                    // Soft delete pharmacy order item

/api/v1/dispense-logs/
├── GET /                           // List dispense logs
├── POST /                          // Create dispense log
├── GET /{id}                       // Get dispense log by ID
├── PUT /{id}                       // Update dispense log
└── DELETE /{id}                    // Soft delete dispense log

/api/v1/adverse-events/
├── GET /                           // List adverse events
├── POST /                          // Create adverse event
├── GET /{id}                       // Get adverse event by ID
├── PUT /{id}                       // Update adverse event
└── DELETE /{id}                    // Soft delete adverse event
```

## 13. Inventory & Procurement
```javascript
/api/v1/inventory-items/
├── GET /                           // List inventory items
├── POST /                          // Create inventory item
├── GET /{id}                       // Get inventory item by ID
├── PUT /{id}                       // Update inventory item
└── DELETE /{id}                    // Soft delete inventory item

/api/v1/inventory-stocks/
├── GET /                           // List inventory stocks
├── POST /                          // Create inventory stock
├── GET /{id}                       // Get inventory stock by ID
├── PUT /{id}                       // Update inventory stock
└── DELETE /{id}                    // Soft delete inventory stock

/api/v1/stock-movements/
├── GET /                           // List stock movements
├── POST /                          // Create stock movement
├── GET /{id}                       // Get stock movement by ID
├── PUT /{id}                       // Update stock movement
└── DELETE /{id}                    // Soft delete stock movement

/api/v1/suppliers/
├── GET /                           // List suppliers
├── POST /                          // Create supplier
├── GET /{id}                       // Get supplier by ID
├── PUT /{id}                       // Update supplier
└── DELETE /{id}                    // Soft delete supplier

/api/v1/purchase-requests/
├── GET /                           // List purchase requests
├── POST /                          // Create purchase request
├── GET /{id}                       // Get purchase request by ID
├── PUT /{id}                       // Update purchase request
└── DELETE /{id}                    // Soft delete purchase request

/api/v1/purchase-orders/
├── GET /                           // List purchase orders
├── POST /                          // Create purchase order
├── GET /{id}                       // Get purchase order by ID
├── PUT /{id}                       // Update purchase order
└── DELETE /{id}                    // Soft delete purchase order

/api/v1/goods-receipts/
├── GET /                           // List goods receipts
├── POST /                          // Create goods receipt
├── GET /{id}                       // Get goods receipt by ID
├── PUT /{id}                       // Update goods receipt
└── DELETE /{id}                    // Soft delete goods receipt

/api/v1/stock-adjustments/
├── GET /                           // List stock adjustments
├── POST /                          // Create stock adjustment
├── GET /{id}                       // Get stock adjustment by ID
├── PUT /{id}                       // Update stock adjustment
└── DELETE /{id}                    // Soft delete stock adjustment
```

## 14. Emergency & Ambulance
```javascript
/api/v1/emergency-cases/
├── GET /                           // List emergency cases
├── POST /                          // Create emergency case
├── GET /{id}                       // Get emergency case by ID
├── PUT /{id}                       // Update emergency case
└── DELETE /{id}                    // Soft delete emergency case

/api/v1/triage-assessments/
├── GET /                           // List triage assessments
├── POST /                          // Create triage assessment
├── GET /{id}                       // Get triage assessment by ID
├── PUT /{id}                       // Update triage assessment
└── DELETE /{id}                    // Soft delete triage assessment

/api/v1/emergency-responses/
├── GET /                           // List emergency responses
├── POST /                          // Create emergency response
├── GET /{id}                       // Get emergency response by ID
├── PUT /{id}                       // Update emergency response
└── DELETE /{id}                    // Soft delete emergency response

/api/v1/ambulances/
├── GET /                           // List ambulances
├── POST /                          // Create ambulance
├── GET /{id}                       // Get ambulance by ID
├── PUT /{id}                       // Update ambulance
└── DELETE /{id}                    // Soft delete ambulance

/api/v1/ambulance-dispatches/
├── GET /                           // List ambulance dispatches
├── POST /                          // Create ambulance dispatch
├── GET /{id}                       // Get ambulance dispatch by ID
├── PUT /{id}                       // Update ambulance dispatch
└── DELETE /{id}                    // Soft delete ambulance dispatch

/api/v1/ambulance-trips/
├── GET /                           // List ambulance trips
├── POST /                          // Create ambulance trip
├── GET /{id}                       // Get ambulance trip by ID
├── PUT /{id}                       // Update ambulance trip
└── DELETE /{id}                    // Soft delete ambulance trip
```

## 15. Billing, Payments & Insurance
```javascript
/api/v1/invoices/
├── GET /                           // List invoices
├── POST /                          // Create invoice
├── GET /{id}                       // Get invoice by ID
├── PUT /{id}                       // Update invoice
└── DELETE /{id}                    // Soft delete invoice

/api/v1/invoice-items/
├── GET /                           // List invoice items
├── POST /                          // Create invoice item
├── GET /{id}                       // Get invoice item by ID
├── PUT /{id}                       // Update invoice item
└── DELETE /{id}                    // Soft delete invoice item

/api/v1/payments/
├── GET /                           // List payments
├── POST /                          // Create payment
├── GET /{id}                       // Get payment by ID
├── PUT /{id}                       // Update payment
└── DELETE /{id}                    // Soft delete payment

/api/v1/refunds/
├── GET /                           // List refunds
├── POST /                          // Create refund
├── GET /{id}                       // Get refund by ID
├── PUT /{id}                       // Update refund
└── DELETE /{id}                    // Soft delete refund

/api/v1/pricing-rules/
├── GET /                           // List pricing rules
├── POST /                          // Create pricing rule
├── GET /{id}                       // Get pricing rule by ID
├── PUT /{id}                       // Update pricing rule
└── DELETE /{id}                    // Soft delete pricing rule

/api/v1/coverage-plans/
├── GET /                           // List coverage plans
├── POST /                          // Create coverage plan
├── GET /{id}                       // Get coverage plan by ID
├── PUT /{id}                       // Update coverage plan
└── DELETE /{id}                    // Soft delete coverage plan

/api/v1/insurance-claims/
├── GET /                           // List insurance claims
├── POST /                          // Create insurance claim
├── GET /{id}                       // Get insurance claim by ID
├── PUT /{id}                       // Update insurance claim
└── DELETE /{id}                    // Soft delete insurance claim

/api/v1/pre-authorizations/
├── GET /                           // List pre-authorizations
├── POST /                          // Create pre-authorization
├── GET /{id}                       // Get pre-authorization by ID
├── PUT /{id}                       // Update pre-authorization
└── DELETE /{id}                    // Soft delete pre-authorization

/api/v1/billing-adjustments/
├── GET /                           // List billing adjustments
├── POST /                          // Create billing adjustment
├── GET /{id}                       // Get billing adjustment by ID
├── PUT /{id}                       // Update billing adjustment
└── DELETE /{id}                    // Soft delete billing adjustment
```

## 16. HR, Payroll & Staffing
```javascript
/api/v1/staff-profiles/
├── GET /                           // List staff profiles
├── POST /                          // Create staff profile
├── GET /{id}                       // Get staff profile by ID
├── PUT /{id}                       // Update staff profile
└── DELETE /{id}                    // Soft delete staff profile

/api/v1/staff-assignments/
├── GET /                           // List staff assignments
├── POST /                          // Create staff assignment
├── GET /{id}                       // Get staff assignment by ID
├── PUT /{id}                       // Update staff assignment
└── DELETE /{id}                    // Soft delete staff assignment

/api/v1/staff-leaves/
├── GET /                           // List staff leaves
├── POST /                          // Create staff leave
├── GET /{id}                       // Get staff leave by ID
├── PUT /{id}                       // Update staff leave
└── DELETE /{id}                    // Soft delete staff leave

/api/v1/shifts/
├── GET /                           // List shifts
├── POST /                          // Create shift
├── GET /{id}                       // Get shift by ID
├── PUT /{id}                       // Update shift
├── DELETE /{id}                    // Soft delete shift
└── POST /{id}/publish              // Publish shift schedule

/api/v1/shift-assignments/
├── GET /                           // List shift assignments
├── POST /                          // Create shift assignment
├── GET /{id}                       // Get shift assignment by ID
├── PUT /{id}                       // Update shift assignment
└── DELETE /{id}                    // Soft delete shift assignment

/api/v1/shift-swap-requests/
├── GET /                           // List shift swap requests
├── POST /                          // Create shift swap request
├── GET /{id}                       // Get shift swap request by ID
├── PUT /{id}                       // Update shift swap request
└── DELETE /{id}                    // Soft delete shift swap request

/api/v1/payroll-runs/
├── GET /                           // List payroll runs
├── POST /                          // Create payroll run
├── GET /{id}                       // Get payroll run by ID
├── PUT /{id}                       // Update payroll run
└── DELETE /{id}                    // Soft delete payroll run

/api/v1/payroll-items/
├── GET /                           // List payroll items
├── POST /                          // Create payroll item
├── GET /{id}                       // Get payroll item by ID
├── PUT /{id}                       // Update payroll item
└── DELETE /{id}                    // Soft delete payroll item
```

## 17. Housekeeping & Facilities
```javascript
/api/v1/housekeeping-tasks/
├── GET /                           // List housekeeping tasks
├── POST /                          // Create housekeeping task
├── GET /{id}                       // Get housekeeping task by ID
├── PUT /{id}                       // Update housekeeping task
└── DELETE /{id}                    // Soft delete housekeeping task

/api/v1/housekeeping-schedules/
├── GET /                           // List housekeeping schedules
├── POST /                          // Create housekeeping schedule
├── GET /{id}                       // Get housekeeping schedule by ID
├── PUT /{id}                       // Update housekeeping schedule
└── DELETE /{id}                    // Soft delete housekeeping schedule

/api/v1/maintenance-requests/
├── GET /                           // List maintenance requests
├── POST /                          // Create maintenance request
├── GET /{id}                       // Get maintenance request by ID
├── PUT /{id}                       // Update maintenance request
└── DELETE /{id}                    // Soft delete maintenance request

/api/v1/assets/
├── GET /                           // List assets
├── POST /                          // Create asset
├── GET /{id}                       // Get asset by ID
├── PUT /{id}                       // Update asset
└── DELETE /{id}                    // Soft delete asset

/api/v1/asset-service-logs/
├── GET /                           // List asset service logs
├── POST /                          // Create asset service log
├── GET /{id}                       // Get asset service log by ID
├── PUT /{id}                       // Update asset service log
└── DELETE /{id}                    // Soft delete asset service log
```

## 18. Notifications & Communications
```javascript
/api/v1/notifications/
├── GET /                           // List notifications
├── POST /                          // Create notification
├── GET /{id}                       // Get notification by ID
├── PUT /{id}                       // Update notification
└── DELETE /{id}                    // Soft delete notification

/api/v1/notification-deliveries/
├── GET /                           // List notification deliveries
├── POST /                          // Create notification delivery
├── GET /{id}                       // Get notification delivery by ID
├── PUT /{id}                       // Update notification delivery
└── DELETE /{id}                    // Soft delete notification delivery

/api/v1/conversations/
├── GET /                           // List conversations
├── POST /                          // Create conversation
├── GET /{id}                       // Get conversation by ID
├── PUT /{id}                       // Update conversation
└── DELETE /{id}                    // Soft delete conversation

/api/v1/messages/
├── GET /                           // List messages
├── POST /                          // Create message
├── GET /{id}                       // Get message by ID
├── PUT /{id}                       // Update message
└── DELETE /{id}                    // Soft delete message

/api/v1/templates/
├── GET /                           // List templates
├── POST /                          // Create template
├── GET /{id}                       // Get template by ID
├── PUT /{id}                       // Update template
└── DELETE /{id}                    // Soft delete template

/api/v1/template-variables/
├── GET /                           // List template variables
├── POST /                          // Create template variable
├── GET /{id}                       // Get template variable by ID
├── PUT /{id}                       // Update template variable
└── DELETE /{id}                    // Soft delete template variable
```

## 19. Reporting & Analytics
```javascript
/api/v1/report-definitions/
├── GET /                           // List report definitions
├── POST /                          // Create report definition
├── GET /{id}                       // Get report definition by ID
├── PUT /{id}                       // Update report definition
└── DELETE /{id}                    // Soft delete report definition

/api/v1/report-runs/
├── GET /                           // List report runs
├── POST /                          // Create report run
├── GET /{id}                       // Get report run by ID
├── PUT /{id}                       // Update report run
└── DELETE /{id}                    // Soft delete report run

/api/v1/dashboard-widgets/
├── GET /                           // List dashboard widgets
├── POST /                          // Create dashboard widget
├── GET /{id}                       // Get dashboard widget by ID
├── PUT /{id}                       // Update dashboard widget
└── DELETE /{id}                    // Soft delete dashboard widget

/api/v1/kpi-snapshots/
├── GET /                           // List KPI snapshots
├── POST /                          // Create KPI snapshot
├── GET /{id}                       // Get KPI snapshot by ID
├── PUT /{id}                       // Update KPI snapshot
└── DELETE /{id}                    // Soft delete KPI snapshot

/api/v1/analytics-events/
├── GET /                           // List analytics events
├── POST /                          // Create analytics event
├── GET /{id}                       // Get analytics event by ID
├── PUT /{id}                       // Update analytics event
└── DELETE /{id}                    // Soft delete analytics event
```

## 20. Subscriptions, Licensing & Modules
```javascript
/api/v1/subscription-plans/
├── GET /                           // List subscription plans
├── POST /                          // Create subscription plan
├── GET /{id}                       // Get subscription plan by ID
├── PUT /{id}                       // Update subscription plan
└── DELETE /{id}                    // Soft delete subscription plan

/api/v1/subscriptions/
├── GET /                           // List subscriptions
├── POST /                          // Create subscription
├── GET /{id}                       // Get subscription by ID
├── PUT /{id}                       // Update subscription
└── DELETE /{id}                    // Soft delete subscription

/api/v1/subscription-invoices/
├── GET /                           // List subscription invoices
├── POST /                          // Create subscription invoice
├── GET /{id}                       // Get subscription invoice by ID
├── PUT /{id}                       // Update subscription invoice
└── DELETE /{id}                    // Soft delete subscription invoice

/api/v1/modules/
├── GET /                           // List modules
├── POST /                          // Create module
├── GET /{id}                       // Get module by ID
├── PUT /{id}                       // Update module
└── DELETE /{id}                    // Soft delete module

/api/v1/module-subscriptions/
├── GET /                           // List module subscriptions
├── POST /                          // Create module subscription
├── GET /{id}                       // Get module subscription by ID
├── PUT /{id}                       // Update module subscription
└── DELETE /{id}                    // Soft delete module subscription

/api/v1/licenses/
├── GET /                           // List licenses
├── POST /                          // Create license
├── GET /{id}                       // Get license by ID
├── PUT /{id}                       // Update license
└── DELETE /{id}                    // Soft delete license
```

## 21. Compliance, Audit & Security
```javascript
/api/v1/audit-logs/
├── GET /                           // List audit logs
└── GET /{id}                       // Get audit log by ID

/api/v1/phi-access-logs/
├── GET /                           // List PHI access logs
├── POST /                          // Create PHI access log
├── GET /{id}                       // Get PHI access log by ID
└── GET /user/{userId}              // Get user PHI access logs

/api/v1/data-processing-logs/
├── GET /                           // List data processing logs
├── POST /                          // Create data processing log
├── GET /{id}                       // Get data processing log by ID
├── PUT /{id}                       // Update data processing log
└── DELETE /{id}                    // Soft delete data processing log

/api/v1/breach-notifications/
├── GET /                           // List breach notifications
├── POST /                          // Report breach
├── GET /{id}                       // Get breach notification by ID
├── PUT /{id}                       // Update breach notification
└── POST /{id}/resolve              // Resolve breach notification

/api/v1/system-change-logs/
├── GET /                           // List system change logs
├── POST /                          // Create system change log
├── GET /{id}                       // Get system change log by ID
├── PUT /{id}                       // Update system change log
├── POST /{id}/approve              // Approve system change
└── POST /{id}/implement            // Implement system change
```

## 22. Integrations & Webhooks
```javascript
/api/v1/integrations/
├── GET /                           // List integrations
├── POST /                          // Create integration
├── GET /{id}                       // Get integration by ID
├── PUT /{id}                       // Update integration
└── DELETE /{id}                    // Soft delete integration

/api/v1/integration-logs/
├── GET /                           // List integration logs
├── GET /{id}                       // Get integration log by ID
└── GET /integration/{integrationId} // Get integration logs by integration

/api/v1/webhook-subscriptions/
├── GET /                           // List webhook subscriptions
├── POST /                          // Create webhook subscription
├── GET /{id}                       // Get webhook subscription by ID
├── PUT /{id}                       // Update webhook subscription
└── DELETE /{id}                    // Soft delete webhook subscription
```

## 23. Telemedicine & Patient Engagement (Feature Mapped)
Telemedicine, patient portal, and engagement features reuse existing resources:
`appointments`, `encounters`, `messages`, `clinical-notes`, `lab-results`,
`radiology-results`, `prescriptions` (via pharmacy orders), and `notifications`.

## 24. Final Enforcement Rule
❌ Any API module endpoint under `/api/v1/` that is not defined in this file **invalid**  
✅ All refactors must **improve compliance**, never reduce it  
This file is **non-negotiable** and applies globally.
---
alwaysApply: true
---

# API Endpoints Reference

## Overview
This document defines the standard API endpoint patterns and structure for Express.js backend applications.
All API module endpoints **must** use the `/api/v1/` base path prefix as required by `api.mdc`.

## 0. System Health Endpoints (Unversioned)
These endpoints are **system endpoints** and are not mounted under `/api/v1` (see `health-checks.mdc`):
```javascript
GET /health
GET /ready
GET /live
```

## 1. Authentication & User Management
```javascript
/api/v1/auth/
├── POST /login                    // Authenticate user and return JWT
├── POST /register                 // Register new user account
├── POST /verify-email             // Verify email with token/code
├── POST /verify-phone             // Verify phone with token
├── POST /forgot-password          // Send password reset email
├── POST /reset-password           // Reset password with token
├── POST /change-password           // Change password (authenticated)
├── POST /refresh                  // Refresh JWT token
├── POST /logout                   // Logout and invalidate session
├── GET /me                        // Get current user info
└── POST /resend-verification      // Resend verification email/SMS

/api/v1/users/
├── GET /                          // List users with pagination
├── POST /                         // Create user (admin only)
├── GET /{id}                      // Get user by ID
├── PUT /{id}                      // Update user
├── DELETE /{id}                   // Soft delete user
├── GET /{id}/orders               // Get user's orders
├── GET /{id}/carts                // Get user's carts
├── GET /{id}/favorites            // Get user's favorites
├── GET /{id}/addresses            // Get user's addresses
├── GET /{id}/notifications        // Get user's notifications
├── GET /{id}/sessions             // Get user's sessions
├── GET /{id}/api-keys             // Get user's API keys
├── GET /{id}/subscriptions        // Get user's subscriptions
├── GET /{id}/consents             // Get user's consents
└── GET /{id}/audit-logs           // Get user's audit logs
```

## 2. Company & Address Management
```javascript
/api/v1/companies/
├── GET /                          // List companies with pagination
├── POST /                         // Create company
├── GET /{id}                      // Get company by ID
├── PUT /{id}                      // Update company
├── DELETE /{id}                   // Soft delete company
├── GET /{id}/users                // Get company users
├── POST /{id}/users               // Add user to company
├── DELETE /{id}/users/{userId}    // Remove user from company
├── GET /{id}/addresses            // Get company addresses
├── GET /{id}/shops                // Get company shops
└── GET /{id}/orders               // Get company orders

/api/v1/addresses/
├── GET /                          // List addresses with pagination
├── POST /                         // Create address
├── GET /{id}                      // Get address by ID
├── PUT /{id}                      // Update address
├── DELETE /{id}                   // Soft delete address
├── POST /{id}/set-default         // Set address as default
└── GET /default/{type}            // Get default address by type
```

## 3. Product Catalog & Inventory
```javascript
/api/v1/categories/
├── GET /                          // List categories with pagination
├── POST /                         // Create category
├── GET /{id}                      // Get category by ID
├── PUT /{id}                      // Update category
├── DELETE /{id}                   // Soft delete category
├── GET /{id}/products             // Get category products
└── GET /{id}/children             // Get subcategories

/api/v1/manufacturers/
├── GET /                          // List manufacturers with pagination
├── POST /                         // Create manufacturer
├── GET /{id}                      // Get manufacturer by ID
├── PUT /{id}                      // Update manufacturer
├── DELETE /{id}                   // Soft delete manufacturer
└── GET /{id}/products             // Get manufacturer products

/api/v1/departments/
├── GET /                          // List departments with pagination
├── POST /                         // Create department
├── GET /{id}                      // Get department by ID
├── PUT /{id}                      // Update department
├── DELETE /{id}                   // Soft delete department
├── GET /{id}/products             // Get department products
└── GET /{id}/media                // Get department media

/api/v1/products/
├── GET /                          // List products with pagination
├── GET /search                    // Search products with fulltext search
├── POST /                         // Create product
├── GET /{id}                      // Get product by ID
├── PUT /{id}                      // Update product
├── DELETE /{id}                   // Soft delete product
├── POST /{id}/publish             // Publish product
├── GET /{id}/attributes           // Get product attributes
├── POST /{id}/attributes          // Add product attribute
├── GET /{id}/media                // Get product media
├── POST /{id}/media               // Add product media
├── GET /{id}/suppliers            // Get product suppliers
├── POST /{id}/suppliers           // Add product supplier
├── GET /{id}/compatibility        // Get product compatibility
├── GET /{id}/price-history        // Get product price history
├── GET /{id}/comments             // Get product comments
├── POST /{id}/comments            // Add product comment
└── GET /{id}/inventory            // Get product inventory
```

## 4. Inventory Management
```javascript
/api/v1/warehouses/
├── GET /                          // List warehouses with pagination
├── POST /                         // Create warehouse
├── GET /{id}                      // Get warehouse by ID
├── PUT /{id}                      // Update warehouse
├── DELETE /{id}                   // Soft delete warehouse
└── GET /{id}/inventory            // Get warehouse inventory

/api/v1/inventory/
├── GET /                          // List inventory with pagination
├── POST /                         // Create inventory record
├── GET /{id}                      // Get inventory by ID
├── PUT /{id}                      // Update inventory
├── DELETE /{id}                   // Soft delete inventory
├── POST /{id}/adjust              // Adjust inventory quantity
└── GET /by-location/{warehouseId}/{productId}/{batchLotId}

/api/v1/batch-lots/
├── GET /                          // List batch lots with pagination
├── POST /                         // Create batch lot
├── GET /{id}                      // Get batch lot by ID
├── PUT /{id}                      // Update batch lot
├── DELETE /{id}                   // Soft delete batch lot
├── GET /{id}/inventory            // Get batch lot inventory
└── GET /{id}/transactions         // Get batch lot transactions
```

## 5. E-commerce & Shopping
```javascript
/api/v1/carts/
├── GET /                          // List carts with pagination
├── POST /                         // Create cart
├── GET /{id}                      // Get cart by ID
├── PUT /{id}                      // Update cart
├── DELETE /{id}                   // Soft delete cart
├── GET /{id}/items                // Get cart items
├── POST /{id}/items               // Add item to cart
├── PUT /{id}/items/{productId}    // Update cart item
├── DELETE /{id}/items/{productId} // Remove item from cart
└── POST /{id}/request-quote       // Request quote for cart

/api/v1/quote-requests/
├── GET /                          // List quote requests with pagination
├── POST /                         // Create quote request
├── GET /{id}                      // Get quote request by ID
├── PUT /{id}                      // Update quote request
├── DELETE /{id}                   // Soft delete quote request
├── GET /{id}/items                // Get quote request items
├── POST /{id}/items               // Add item to quote request
├── GET /{id}/comments             // Get quote request comments
├── POST /{id}/comments            // Add comment to quote request
├── GET /{id}/media                // Get quote request media
├── POST /{id}/media               // Add media to quote request
└── POST /{id}/accept              // Accept quote request

/api/v1/orders/
├── GET /                          // List orders with pagination
├── POST /                         // Create order
├── GET /{id}                      // Get order by ID
├── PUT /{id}                      // Update order
├── DELETE /{id}                   // Soft delete order
├── GET /{id}/items                // Get order items
├── GET /{id}/payments             // Get order payments
├── GET /{id}/shipments            // Get order shipments
├── GET /{id}/returns              // Get order returns
├── POST /{id}/cancel              // Cancel order
└── GET /{id}/tracking             // Get order tracking
```

## 6. Payments & Shipping
```javascript
/api/v1/payments/
├── GET /                          // List payments with pagination
├── POST /                         // Create payment
├── GET /{id}                      // Get payment by ID
├── PUT /{id}                      // Update payment
├── DELETE /{id}                   // Soft delete payment
├── GET /{id}/refunds              // Get payment refunds
└── POST /{id}/refund              // Process refund

/api/v1/shipments/
├── GET /                          // List shipments with pagination
├── POST /                         // Create shipment
├── GET /{id}                      // Get shipment by ID
├── PUT /{id}                      // Update shipment
├── DELETE /{id}                   // Soft delete shipment
├── GET /{id}/items                // Get shipment items
├── GET /{id}/events               // Get shipment events
└── POST /{id}/track               // Track shipment

/api/v1/returns/
├── GET /                          // List returns with pagination
├── POST /                         // Create return
├── GET /{id}                      // Get return by ID
├── PUT /{id}                      // Update return
├── DELETE /{id}                   // Soft delete return
├── GET /{id}/items                // Get return items
└── POST /{id}/approve             // Approve return
```

## 7. Media Asset Management
```javascript
/api/v1/media-assets/
├── GET /                          // List media assets with pagination
├── POST /                         // Upload media asset
├── GET /{id}                      // Get media asset by ID
├── PUT /{id}                      // Update media asset
├── DELETE /{id}                   // Soft delete media asset
├── POST /{id}/review              // Review media asset
└── GET /{id}/download             // Download media asset
```

## 8. Communication & Notifications
```javascript
/api/v1/notifications/
├── GET /                          // List notifications with pagination
├── POST /                         // Create notification
├── GET /{id}                      // Get notification by ID
├── PUT /{id}                      // Update notification
├── DELETE /{id}                   // Soft delete notification
├── POST /{id}/read                // Mark notification as read
├── POST /{id}/unread              // Mark notification as unread
└── GET /targets                   // Get notification targets

/api/v1/conversations/
├── GET /                          // List conversations with pagination
├── POST /                         // Create conversation
├── GET /{id}                      // Get conversation by ID
├── PUT /{id}                      // Update conversation
├── DELETE /{id}                   // Soft delete conversation
├── GET /{id}/participants         // Get conversation participants
├── POST /{id}/participants        // Add participant to conversation
├── DELETE /{id}/participants/{userId} // Remove participant
├── GET /{id}/messages             // Get conversation messages
└── POST /{id}/messages            // Add message to conversation

/api/v1/messages/
├── GET /                          // List messages with pagination
├── POST /                         // Create message
├── GET /{id}                      // Get message by ID
├── PUT /{id}                      // Update message
├── DELETE /{id}                   // Soft delete message
├── GET /{id}/media                // Get message media
└── GET /conversation/{conversationId} // Get conversation messages
```

## 9. Shop Management
```javascript
/api/v1/shops/
├── GET /                          // List shops with pagination
├── POST /                         // Create shop
├── GET /{id}                      // Get shop by ID
├── PUT /{id}                      // Update shop
├── DELETE /{id}                   // Soft delete shop
├── GET /{id}/products             // Get shop products
├── GET /{id}/media                // Get shop media
├── POST /{id}/media               // Add media to shop
├── DELETE /{id}/media/{mediaId}   // Remove media from shop
├── GET /{id}/quote-requests       // Get shop quote requests
├── GET /{id}/orders               // Get shop orders
└── GET /{id}/owner                // Get shop owner
```

## 10. Subscription Management
```javascript
/api/v1/subscription-plans/
├── GET /                          // List plans with pagination
├── POST /                         // Create plan
├── GET /{id}                      // Get plan by ID
├── PUT /{id}                      // Update plan
├── DELETE /{id}                   // Soft delete plan
├── GET /{id}/features             // Get plan features
├── POST /{id}/features            // Add feature to plan
├── PUT /{id}/features/{featureType} // Update plan feature
├── DELETE /{id}/features/{featureType} // Remove plan feature
├── GET /{id}/subscriptions        // Get plan subscriptions
└── POST /{id}/activate            // Activate plan

/api/v1/subscriptions/
├── GET /                          // List subscriptions with pagination
├── POST /                         // Create subscription
├── GET /{id}                      // Get subscription by ID
├── PUT /{id}                      // Update subscription
├── DELETE /{id}                   // Soft delete subscription
├── POST /{id}/cancel              // Cancel subscription
└── POST /{id}/reactivate          // Reactivate subscription

/api/v1/subscription-invoices/
├── GET /                          // List invoices with pagination
├── POST /                         // Create invoice
├── GET /{id}                      // Get invoice by ID
├── PUT /{id}                      // Update invoice
├── DELETE /{id}                   // Soft delete invoice
├── POST /{id}/pay                 // Pay invoice
└── POST /{id}/void                // Void invoice

/api/v1/subscription-payment-methods/
├── GET /                          // List payment methods with pagination
├── POST /                         // Create payment method
├── GET /{id}                      // Get payment method by ID
├── PUT /{id}                      // Update payment method
├── DELETE /{id}                   // Soft delete payment method
├── POST /{id}/set-default         // Set as default payment method
├── POST /{id}/activate            // Activate payment method
├── POST /{id}/deactivate          // Deactivate payment method
└── GET /user/{userId}             // Get user payment methods

/api/v1/subscription-usage/
├── GET /                          // List usage with pagination
├── POST /                         // Create usage record
├── GET /{id}                      // Get usage record by ID
├── PUT /{id}                      // Update usage record
├── DELETE /{id}                   // Soft delete usage record
├── GET /subscription/{subscriptionId} // Get subscription usage
├── GET /feature/{featureType}     // Get feature usage
└── GET /date/{usageDate}          // Get usage by date
```

## 11. Compliance & Legal
```javascript
/api/v1/data-processing/
├── GET /                          // List processing logs with pagination
├── POST /                         // Create processing log
├── GET /{id}                      // Get processing log by ID
├── PUT /{id}                      // Update processing log
└── DELETE /{id}                   // Soft delete processing log

/api/v1/breach-notifications/
├── GET /                          // List notifications with pagination
├── POST /                         // Report breach
├── GET /{id}                      // Get notification by ID
├── PUT /{id}                      // Update notification
└── POST /{id}/resolve             // Resolve breach notification

/api/v1/phi-access/
├── GET /                          // List PHI access logs with pagination
├── POST /                         // Create access log
├── GET /{id}                      // Get access log by ID
├── GET /user/{userId}             // Get user PHI access
└── GET /type/{phiType}            // Get PHI access by type

/api/v1/financial-controls/
├── GET /                          // List control logs with pagination
├── POST /                         // Create control log
├── GET /{id}                      // Get control log by ID
├── PUT /{id}                      // Update control log
└── GET /type/{controlType}        // Get controls by type

/api/v1/system-changes/
├── GET /                          // List change logs with pagination
├── POST /                         // Create change log
├── GET /{id}                      // Get change log by ID
├── PUT /{id}                      // Update change log
├── POST /{id}/approve             // Approve system change
├── POST /{id}/implement           // Implement system change
└── POST /{id}/rollback            // Rollback system change

/api/v1/consents/
├── GET /                          // List consents with pagination
├── POST /                         // Create consent
├── GET /{id}                      // Get consent by ID
├── PUT /{id}                      // Update consent
├── DELETE /{id}                   // Soft delete consent
└── GET /user/{userId}             // Get user consents

/api/v1/terms-acceptance/
├── GET /                          // List terms acceptances with pagination
├── POST /                         // Create terms acceptance
├── GET /{id}                      // Get terms acceptance by ID
└── GET /user/{userId}             // Get user terms acceptances
```

## 12. Analytics & Reporting
```javascript
/api/v1/audit-logs/
├── GET /                          // List audit logs with pagination
├── GET /{id}                      // Get audit log by ID
├── GET /entity/{entity}/{entityId} // Get entity audit logs
└── GET /user/{userId}             // Get user audit logs

/api/v1/price-history/
├── GET /                          // List price history with pagination
├── GET /{id}                      // Get price history by ID
└── GET /product/{productId}       // Get product price history
```

## 13. Promotions & Marketing
```javascript
/api/v1/promotions/
├── GET /                          // List promotions with pagination
├── POST /                         // Create promotion
├── GET /{id}                      // Get promotion by ID
├── PUT /{id}                      // Update promotion
├── DELETE /{id}                   // Soft delete promotion
└── GET /{id}/redemptions          // Get promotion redemptions

/api/v1/coupon-redemptions/
├── GET /                          // List coupon redemptions with pagination
├── POST /                         // Create coupon redemption
├── GET /{id}                      // Get coupon redemption by ID
└── GET /promotion/{promotionId}   // Get promotion redemptions
```

## 14. User Favorites & Social Links
```javascript
/api/v1/user-favorites/
├── GET /                          // List user favorites with pagination
├── POST /                         // Add favorite
├── DELETE /{id}                   // Remove favorite
├── GET /user/{userId}             // Get user favorites
└── GET /product/{productId}       // Get product favorites

/api/v1/user-social-links/
├── GET /                          // List social links with pagination
├── POST /                         // Create social link
├── GET /{id}                      // Get social link by ID
├── PUT /{id}                      // Update social link
├── DELETE /{id}                   // Soft delete social link
├── GET /user/{userId}             // Get user social links
└── GET /platform/{platform}       // Get platform social links
```

## 15. Reviews & Ratings
```javascript
/api/v1/product-reviews/
├── GET /                          // List product reviews with pagination
├── POST /                         // Create product review
├── GET /{id}                      // Get product review by ID
├── PUT /{id}                      // Update product review
├── DELETE /{id}                   // Soft delete product review
├── GET /product/{productId}       // Get product reviews
└── GET /user/{userId}             // Get user reviews

/api/v1/review-media-assets/
├── GET /                          // List review media assets with pagination
├── POST /                         // Add media to review
├── GET /{id}                      // Get review media asset by ID
├── PUT /{id}                      // Update review media asset
├── DELETE /{id}                   // Soft delete review media asset
└── GET /review/{reviewId}         // Get review media assets

/api/v1/vendor-review-responses/
├── GET /                          // List vendor review responses with pagination
├── POST /                         // Create vendor review response
├── GET /{id}                      // Get vendor review response by ID
├── PUT /{id}                      // Update vendor review response
├── DELETE /{id}                   // Soft delete vendor review response
└── GET /review/{reviewId}         // Get review responses

/api/v1/review-helpful-votes/
├── GET /                          // List review helpful votes with pagination
├── POST /                         // Add helpful vote to review
├── DELETE /{id}                   // Remove helpful vote
├── GET /review/{reviewId}         // Get review helpful votes
└── GET /user/{userId}             // Get user helpful votes
```

## 16. Payment Methods
```javascript
/api/v1/order-payment-methods/
├── GET /                          // List order payment methods with pagination
├── POST /                         // Create order payment method
├── GET /{id}                      // Get order payment method by ID
├── PUT /{id}                      // Update order payment method
├── DELETE /{id}                   // Soft delete order payment method
└── GET /order/{orderId}            // Get order payment methods

/api/v1/payment-method-requests/
├── GET /                          // List payment method requests with pagination
├── POST /                         // Create payment method request
├── GET /{id}                      // Get payment method request by ID
├── PUT /{id}                      // Update payment method request
├── DELETE /{id}                   // Soft delete payment method request
└── GET /user/{userId}             // Get user payment method requests

/api/v1/payment-method-request-upvotes/
├── GET /                          // List payment method request upvotes with pagination
├── POST /                         // Add upvote to payment method request
├── DELETE /{id}                   // Remove upvote
├── GET /request/{requestId}       // Get request upvotes
└── GET /user/{userId}             // Get user upvotes
```

## 17. Approval Workflows
```javascript
/api/v1/product-approvals/
├── GET /                          // List product approvals with pagination
├── POST /                         // Create product approval
├── GET /{id}                      // Get product approval by ID
├── PUT /{id}                      // Update product approval
├── DELETE /{id}                   // Soft delete product approval
├── POST /{id}/approve             // Approve product
├── POST /{id}/reject              // Reject product
└── GET /product/{productId}       // Get product approvals

/api/v1/product-approval-histories/
├── GET /                          // List product approval histories with pagination
├── GET /{id}                      // Get product approval history by ID
├── GET /product/{productId}       // Get product approval history
└── GET /approval/{approvalId}      // Get approval history

/api/v1/vendor-approvals/
├── GET /                          // List vendor approvals with pagination
├── POST /                         // Create vendor approval
├── GET /{id}                      // Get vendor approval by ID
├── PUT /{id}                      // Update vendor approval
├── DELETE /{id}                   // Soft delete vendor approval
├── POST /{id}/approve             // Approve vendor
├── POST /{id}/reject              // Reject vendor
└── GET /shop/{shopId}             // Get shop vendor approvals

/api/v1/vendor-approval-documents/
├── GET /                          // List vendor approval documents with pagination
├── POST /                         // Upload vendor approval document
├── GET /{id}                      // Get vendor approval document by ID
├── PUT /{id}                      // Update vendor approval document
├── DELETE /{id}                   // Soft delete vendor approval document
└── GET /approval/{approvalId}      // Get approval documents

/api/v1/vendor-approval-histories/
├── GET /                          // List vendor approval histories with pagination
├── GET /{id}                      // Get vendor approval history by ID
├── GET /shop/{shopId}             // Get shop approval history
└── GET /approval/{approvalId}      // Get approval history
```

## 18. Product Compliance
```javascript
/api/v1/product-certifications/
├── GET /                          // List product certifications with pagination
├── POST /                         // Create product certification
├── GET /{id}                      // Get product certification by ID
├── PUT /{id}                      // Update product certification
├── DELETE /{id}                   // Soft delete product certification
└── GET /product/{productId}       // Get product certifications

/api/v1/product-hazards/
├── GET /                          // List product hazards with pagination
├── POST /                         // Create product hazard
├── GET /{id}                      // Get product hazard by ID
├── PUT /{id}                      // Update product hazard
├── DELETE /{id}                   // Soft delete product hazard
└── GET /product/{productId}       // Get product hazards
```

## 19. Enhanced Authentication
```javascript
/api/v1/user-mfas/
├── GET /                          // List user MFA configurations with pagination
├── POST /                         // Create user MFA configuration
├── GET /{id}                      // Get user MFA by ID
├── PUT /{id}                      // Update user MFA
├── DELETE /{id}                   // Soft delete user MFA
├── POST /{id}/verify              // Verify MFA code
├── POST /{id}/enable              // Enable MFA
├── POST /{id}/disable             // Disable MFA
└── GET /user/{userId}             // Get user MFA configurations

/api/v1/oauth-accounts/
├── GET /                          // List OAuth accounts with pagination
├── POST /                         // Link OAuth account
├── GET /{id}                      // Get OAuth account by ID
├── PUT /{id}                      // Update OAuth account
├── DELETE /{id}                   // Soft delete OAuth account
└── GET /user/{userId}             // Get user OAuth accounts
```

## 20. Enterprise Payments
```javascript
/api/v1/purchase-orders/
├── GET /                          // List purchase orders with pagination
├── POST /                         // Create purchase order
├── GET /{id}                      // Get purchase order by ID
├── PUT /{id}                      // Update purchase order
├── DELETE /{id}                   // Soft delete purchase order
├── POST /{id}/approve             // Approve purchase order
├── POST /{id}/reject              // Reject purchase order
└── GET /order/{orderId}            // Get order purchase orders

/api/v1/lease-agreements/
├── GET /                          // List lease agreements with pagination
├── POST /                         // Create lease agreement
├── GET /{id}                      // Get lease agreement by ID
├── PUT /{id}                      // Update lease agreement
├── DELETE /{id}                   // Soft delete lease agreement
├── POST /{id}/activate            // Activate lease agreement
├── POST /{id}/terminate            // Terminate lease agreement
└── GET /order/{orderId}            // Get order lease agreements

/api/v1/lease-payments/
├── GET /                          // List lease payments with pagination
├── POST /                         // Create lease payment
├── GET /{id}                      // Get lease payment by ID
├── PUT /{id}                      // Update lease payment
├── DELETE /{id}                   // Soft delete lease payment
└── GET /lease/{leaseId}            // Get lease payments

/api/v1/net-terms/
├── GET /                          // List net terms with pagination
├── POST /                         // Create net terms
├── GET /{id}                      // Get net terms by ID
├── PUT /{id}                      // Update net terms
├── DELETE /{id}                   // Soft delete net terms
└── GET /order/{orderId}            // Get order net terms
```

## 21. Safety & Compliance
```javascript
/api/v1/product-recalls/
├── GET /                          // List product recalls with pagination
├── POST /                         // Create product recall
├── GET /{id}                      // Get product recall by ID
├── PUT /{id}                      // Update product recall
├── DELETE /{id}                   // Soft delete product recall
├── POST /{id}/publish             // Publish product recall
└── GET /product/{productId}       // Get product recalls

/api/v1/recall-notifications/
├── GET /                          // List recall notifications with pagination
├── POST /                         // Create recall notification
├── GET /{id}                      // Get recall notification by ID
├── PUT /{id}                      // Update recall notification
├── DELETE /{id}                   // Soft delete recall notification
└── GET /recall/{recallId}          // Get recall notifications
```

## 22. Integrations & Discovery
```javascript
/api/v1/integration-logs/
├── GET /                          // List integration logs with pagination
├── GET /{id}                      // Get integration log by ID
├── GET /integration/{integrationId} // Get integration logs
└── GET /date/{date}               // Get logs by date

/api/v1/saved-searches/
├── GET /                          // List saved searches with pagination
├── POST /                         // Create saved search
├── GET /{id}                      // Get saved search by ID
├── PUT /{id}                      // Update saved search
├── DELETE /{id}                   // Soft delete saved search
└── GET /user/{userId}             // Get user saved searches

/api/v1/search-history/
├── GET /                          // List search history with pagination
├── GET /{id}                      // Get search history by ID
├── GET /user/{userId}             // Get user search history
└── DELETE /{id}                   // Delete search history entry

/api/v1/wishlists/
├── GET /                          // List wishlists with pagination
├── POST /                         // Create wishlist
├── GET /{id}                      // Get wishlist by ID
├── PUT /{id}                      // Update wishlist
├── DELETE /{id}                   // Soft delete wishlist
└── GET /user/{userId}             // Get user wishlists

/api/v1/wishlist-items/
├── GET /                          // List wishlist items with pagination
├── POST /                         // Add item to wishlist
├── GET /{id}                      // Get wishlist item by ID
├── PUT /{id}                      // Update wishlist item
├── DELETE /{id}                   // Soft delete wishlist item
└── GET /wishlist/{wishlistId}      // Get wishlist items
```

## 23. Marketing & Support
```javascript
/api/v1/referral-codes/
├── GET /                          // List referral codes with pagination
├── POST /                         // Create referral code
├── GET /{id}                      // Get referral code by ID
├── PUT /{id}                      // Update referral code
├── DELETE /{id}                   // Soft delete referral code
├── GET /user/{userId}             // Get user referral codes
└── GET /code/{code}               // Get referral code by code string

/api/v1/referrals/
├── GET /                          // List referrals with pagination
├── POST /                         // Create referral
├── GET /{id}                      // Get referral by ID
├── PUT /{id}                      // Update referral
├── DELETE /{id}                   // Soft delete referral
├── GET /referrer/{userId}         // Get referrals by referrer
└── GET /referee/{userId}          // Get referrals by referee

/api/v1/affiliate-links/
├── GET /                          // List affiliate links with pagination
├── POST /                         // Create affiliate link
├── GET /{id}                      // Get affiliate link by ID
├── PUT /{id}                      // Update affiliate link
├── DELETE /{id}                   // Soft delete affiliate link
├── GET /user/{userId}             // Get user affiliate links
└── GET /product/{productId}       // Get product affiliate links

/api/v1/affiliate-commissions/
├── GET /                          // List affiliate commissions with pagination
├── POST /                         // Create affiliate commission
├── GET /{id}                      // Get affiliate commission by ID
├── PUT /{id}                      // Update affiliate commission
├── DELETE /{id}                   // Soft delete affiliate commission
├── GET /affiliate/{affiliateId}    // Get affiliate commissions
└── GET /order/{orderId}            // Get order commissions
```

## 24. Support & Dispute Resolution
```javascript
/api/v1/support-tickets/
├── GET /                          // List support tickets with pagination
├── POST /                         // Create support ticket
├── GET /{id}                      // Get support ticket by ID
├── PUT /{id}                      // Update support ticket
├── DELETE /{id}                   // Soft delete support ticket
├── POST /{id}/assign              // Assign ticket to user
├── POST /{id}/close               // Close support ticket
├── POST /{id}/reopen              // Reopen support ticket
└── GET /user/{userId}             // Get user support tickets

/api/v1/support-ticket-comments/
├── GET /                          // List support ticket comments with pagination
├── POST /                         // Add comment to support ticket
├── GET /{id}                      // Get support ticket comment by ID
├── PUT /{id}                      // Update support ticket comment
├── DELETE /{id}                   // Soft delete support ticket comment
└── GET /ticket/{ticketId}          // Get ticket comments

/api/v1/support-ticket-histories/
├── GET /                          // List support ticket histories with pagination
├── GET /{id}                      // Get support ticket history by ID
├── GET /ticket/{ticketId}         // Get ticket history
└── GET /user/{userId}             // Get user ticket histories

/api/v1/disputes/
├── GET /                          // List disputes with pagination
├── POST /                         // Create dispute
├── GET /{id}                      // Get dispute by ID
├── PUT /{id}                      // Update dispute
├── DELETE /{id}                   // Soft delete dispute
├── POST /{id}/resolve             // Resolve dispute
├── POST /{id}/escalate            // Escalate dispute
└── GET /order/{orderId}            // Get order disputes

/api/v1/dispute-comments/
├── GET /                          // List dispute comments with pagination
├── POST /                         // Add comment to dispute
├── GET /{id}                      // Get dispute comment by ID
├── PUT /{id}                      // Update dispute comment
├── DELETE /{id}                   // Soft delete dispute comment
└── GET /dispute/{disputeId}        // Get dispute comments

/api/v1/dispute-histories/
├── GET /                          // List dispute histories with pagination
├── GET /{id}                      // Get dispute history by ID
├── GET /dispute/{disputeId}        // Get dispute history
└── GET /user/{userId}             // Get user dispute histories
```

## 25. Loyalty Program
```javascript
/api/v1/loyalty-points/
├── GET /                          // List loyalty points with pagination
├── POST /                         // Create loyalty points
├── GET /{id}                      // Get loyalty points by ID
├── PUT /{id}                      // Update loyalty points
├── DELETE /{id}                   // Soft delete loyalty points
└── GET /user/{userId}             // Get user loyalty points

/api/v1/loyalty-transactions/
├── GET /                          // List loyalty transactions with pagination
├── POST /                         // Create loyalty transaction
├── GET /{id}                      // Get loyalty transaction by ID
├── PUT /{id}                      // Update loyalty transaction
├── DELETE /{id}                   // Soft delete loyalty transaction
├── GET /user/{userId}             // Get user loyalty transactions
└── GET /points/{pointsId}         // Get points transactions

/api/v1/loyalty-badges/
├── GET /                          // List loyalty badges with pagination
├── POST /                         // Create loyalty badge
├── GET /{id}                      // Get loyalty badge by ID
├── PUT /{id}                      // Update loyalty badge
├── DELETE /{id}                   // Soft delete loyalty badge
└── GET /tier/{tier}                // Get badges by tier

/api/v1/user-loyalty-badges/
├── GET /                          // List user loyalty badges with pagination
├── POST /                         // Assign badge to user
├── GET /{id}                      // Get user loyalty badge by ID
├── DELETE /{id}                   // Remove badge from user
├── GET /user/{userId}             // Get user badges
└── GET /badge/{badgeId}           // Get badge assignments
```

## 26. Final Enforcement Rule
❌ Any API module endpoint under `/api/v1/` that is not defined in this file **invalid**  
✅ All refactors must **improve compliance**, never reduce it  
This file is **non-negotiable** and applies globally.
